Search the web for information about the following query. {{instruction}}

QUERY:
{{input}}

Use the search_web tool to find relevant results. You may search multiple times with refined queries if needed.

Once you have gathered enough information, synthesize your findings into a JSON object matching this schema:
{{schema}}
